# マイライフログ 引継ぎ資料

このリポジトリで作業する際に最初に読むファイル。
2026年8月〜9月に Claude（Cowork）側で行った設計・実装の背景をまとめてある。

---

## 1. これは何か

個人用のライフログアプリ置き場。GitHub Pages で配信している静的サイトで、
サーバーもビルドもない。HTML / CSS / 素の JavaScript だけ。

- 公開URL: `https://19tmnr89-design.github.io/life-log/`
- リポジトリ: `19tmnr89-design/life-log`（**public**）
- 利用者は本人1人。PC（Chrome）とスマホ（ホーム画面に追加して使用）の2台。

現在2つのアプリが入っている。

| アプリ | 入口 | 状態 |
|---|---|---|
| 筋トレログ | `kintore.html` | 以前から稼働中。今回は触っていない |
| お金ログ | `money.html` | 2026年8月に新規作成。家計簿と資産管理 |

### ファイル構成

リポジトリ直下にフラットに置く規約。サブディレクトリは作らない。

```
index.html          アプリ選択のトップ（カードを並べるだけ）
style.css           index.html 用
sync.js             端末間同期。両アプリ共通
firebase-config.js  Firebase の設定（同期に使用）
kintore.html        筋トレログ
app.js              筋トレログのロジック
money.html          お金ログ
money.app.js        お金ログのロジック（約1,170行）
money.css           お金ログのスタイル
import_template.csv 筋トレログ用の取り込みテンプレート
```

命名は `<アプリ名>.html` + `<アプリ名>.app.js` + `<アプリ名>.css`。
新しいアプリを足すときもこれに合わせる。

---

## 2. お金ログの画面

下部タブで5画面。`.screen` の `on` クラスを付け替えて切り替える（SPA的な遷移はない）。

| タブ | id | 役割 |
|---|---|---|
| ✏️ 入力 | `s-entry` | 支出・収入を1件ずつ記録。テンキー、日付チップ、大項目→内容の2段選択 |
| 🔁 定例 | `s-fixed` | 毎月20日ごろにまとめて入れる項目を一覧で一括記録 |
| 📊 資産 | `s-assets` | 区分別の積み上げ面グラフ＋残高入力 |
| 📉 支出 | `s-log` | `[推移]` 月次の積み上げ棒グラフ＋収入折れ線 / `[履歴]` 記録一覧 |
| 💾 データ | `s-data` | CSV取り込み、JSON書き出し、同期設定、全削除 |

記録をタップすると修正シート（`#editSheet`）が開き、日付・金額・大項目・内容・補足を直せる。

### 入力画面の設計意図

- テンキーは金額を打つときだけ出す。出しっぱなしだとカテゴリのタイルが隠れて選べない。
- PC（`matchMedia("(pointer:fine)")`）では数字キーで直接打てる。Backspace で1文字削除、Esc でクリア。
  文字入力欄にフォーカスがあるときと修正シートを開いている間は無効。
- 大項目を選ぶとその大項目の内容が全部出る。内容を直接選ぶと大項目は自動で決まる。
- 日付の既定値は当日。`‹ ›` で前後の日に動かせる。

---

## 3. データの形

**唯一の出所は `localStorage["money-log-v1"]` に入っている1本の配列。**
種類の違うレコードが `k` で区別されて同居している。

```
取引   {k:"t",  id, d:"YYYY-MM-DD", s:内容, m:円, n?:補足, t?:"i"（収入のときだけ）}
分類   {k:"map",id, v:{内容:[大項目, 中項目, リベ分類]}}
資産   {k:"a",  id, d:"YYYY-MM-01", a:口座id, m:円}
口座   {k:"acc",id, a:口座id, name, grp:表示区分, active:1|0, ord}
目標   {k:"tgt",id, a:口座id, m:円}
設定   {k:"cfg",id, key:"fixed", pin:[内容], hide:[内容]}
```

### なぜ取引に分類を持たせていないか

素直に作ると取引1件ごとに大項目・中項目・リベ分類の3列を持つが、
それだと Firestore の1ドキュメント上限 1MiB を **1.22MiB で超えた**。

実データ90種類の「内容」を調べたところ、内容が決まれば分類は一意に決まる（例外ゼロ）。
そこで分類は `{k:"map"}` 1本に寄せ、IDを8桁に短縮し、既定値（支出・補足なし）を省いた。
結果 **719KB（上限の70%）** に収まっている。

⚠️ **年に90KB前後ずつ増える。あと3年ほどで上限に近づく。**
データ画面に現在のデータ量を出しているので、80%を超えたら次のどちらかで対応する。

- `sync.js` を年ごとの分割保存に対応させる（`money-2020`, `money-2021`… と複数ドキュメントに分ける）
- 古い年の取引を書き出して外し、資産推移と直近数年だけ同期する

**上限に達すると同期が静かに失敗する**ので、放置すると気づけない。

### 口座・カテゴリ・定例項目をコードに書かない

このリポジトリは公開なので、口座名・費目名・定例項目はコードに一切置いていない。
すべて取り込んだ記録から実行時に組み立てている。

- 口座: `{k:"acc"}` レコードから
- カテゴリ（大項目→内容）: 記録済みの取引から組み立て、使用頻度順に並べる
- 定例項目: 記録の日付パターンから自動判定（後述）

コードに入っている固定文字列は、取り込み前に使う一般的な費目（食料品・外食・家賃・電気代など）だけ。

---

## 4. 同期の仕組み

`sync.js` が Firebase Firestore と繋ぐ。両アプリ共通。

```html
<script src="money.app.js"></script>
<script>window.SYNC_APP = "money";</script>
<script type="module" src="sync.js"></script>
```

- 読み込み順が重要。`money.app.js` → `SYNC_APP` の設定 → `sync.js`（module）。
- ドキュメントID = `sha256(SYNC_APP + ":" + 同期コード)`、コレクション名 = `SYNC_APP`。
- 匿名認証。初回リンク時にリモートとローカルを**和集合でマージ**してから購読を始める。
- アプリ側は `money:changed` / `money:remote` イベントで再描画する。

### `SYNC_FINGERPRINT`

和集合マージの重複判定に使う指紋関数。`sync.js` のデフォルトは筋トレ用
（`date + exercise + sets`）で、お金のレコードには `exercise` も `sets` も無く
`"2026-08-27 undefined "` に潰れてしまう。同じ日の記録が1件しか残らない事故になる。

そのため `sync.js` に2行足して、アプリ側が指定できるようにしてある。

```js
function recordFingerprint(r) {
  // アプリ側が指紋関数を指定していればそれを使う（未指定なら従来どおり筋トレ用）
  if (typeof window.SYNC_FINGERPRINT === "function") return window.SYNC_FINGERPRINT(r);
  const sets = (r.sets || []).map(s => `${s.weight}x${s.reps}`).join("|");
  return `${r.date} ${r.exercise} ${sets}`;
}
```

お金ログ側は `money.app.js` の冒頭で `window.SYNC_FINGERPRINT = r => r.id || JSON.stringify(r)` を指定。
**筋トレログは `SYNC_FINGERPRINT` を指定していないので、従来どおり動く。**

### セキュリティ上の注意

**同期コードがドキュメントを守る唯一の鍵。**コードを推測されると家計と資産が読まれる。

- 同期コードはデータ画面の「ランダムなコードを生成」で作る15文字を使う。短い数字は厳禁。
- Firestore のルールで `list`（列挙）を許可してはいけない。匿名認証は誰でも通るので、
  `allow read` と書くと `get` だけでなく `list` も通り、ID を知らなくても中身が引ける。

```
allow get, create, update: if request.auth != null;
allow list, delete: if false;
```

を各コレクションに書き、最後に `allow read, write: if false;` のキャッチオールを置く。

---

## 5. 定例（`s-fixed`）の判定ロジック

毎月20日ごろにまとめて入力する項目（給与、税金、固定費、サブスク、積立など）を
自動で拾って一覧にし、金額だけ直して一括記録する画面。

### 判定条件（2026年9月に緩和済み）

直近13ヶ月を見て、次をすべて満たす内容を定例とみなす。

- 20日前後（**16〜25日**）に記録された月が **5ヶ月以上**
- その内容の総記録数が **「20日前後に出た月数 × 1.8」以下**（＝月1回程度しか出ない）
- 20日前後の最後の記録が、基準月から **3ヶ月以内**（いまも続いているもの）

2つ目の条件があるので、20日にもよく出る「外食」「食料品」のような費目は入らない。
実データで判定すると22項目（収入2件・税金社会保険3件・固定費13件・投資貯蓄4件）になる。

さらに**上の条件で1件も拾えなかった場合は、20日前後の記録がある直近の月の顔ぶれを
そのまま雛形として出す**フォールバックがある。数ヶ月空けても一覧が空にならない。

### 緩和した理由（重要）

もとは「直近12ヶ月のうち10ヶ月以上、18〜22日」という条件だった。
実データで数えると**ちょうど10ヶ月**で、取り込んだ過去データが1ヶ月古くなるだけで
22項目すべてが条件から外れ、一覧がまるごと空になった。
利用者からは「定例を毎回1から設定し直すことになっている」という症状として見えた。

**閾値をデータの境界ぎりぎりに置かないこと。** 同種の判定を足すときも同じ。

### 金額の初期値

`lastAmount(内容, 表示月)` が、その月より前の最新の金額を返す。
前の記録が無ければ期間を問わず最新の記録から持ってくる（新しく足した項目用）。

前々月以前の金額を持ってきたときだけ、項目名の下に「2026/8月の金額」と小さく添えて
古い値だと分かるようにしている。前月の金額のときは何も出さない（全行に出すと邪魔なため）。

### 記録すると設定として覚える

「この月の定例をまとめて記録」を押すと、その顔ぶれを `{k:"cfg", key:"fixed"}` の
`pin` に保存する。一度押せば以降は自動判定の結果に関わらず一覧が固定され、
1から設定し直す事態にならない。各行の ✕ で外すと `hide` に入る。

---

## 6. 元データの出所

Google ドライブにあった `家計簿.xlsx` と `財産.xlsx` を Python（openpyxl）で CSV に変換し、
データ画面から取り込んだ。変換スクリプト（`convert.py`）は口座名を含むためリポジトリには置いていない。

取り込む CSV は4本。

```
money_accounts.csv      id,name,cat,owner,wrap,liquid,active
money_assets.csv        date,account_id,amount_yen
money_tx.csv            date,type,cat,sub,item,amount_yen,kind,freq
money_targets_2027.csv  account_id,target_yen
```

取り込み実績: 口座29件 / 資産残高2,279件（90時点）/ 家計簿7,173件（2020-01〜2026-08）/ 目標29件。

### 変換時に見つけた元データの不備（対応済み）

- 財産.xlsx の合計行の数式が1口座を取りこぼしていた（全90時点でズレ）
- 家計簿.xlsx に金額が空欄の行が9件 → 同じ内容の過去実績の中央値で補完し、補足欄に「(推定)」を付けた
- 内容も金額も空欄の行が1件 → 削除
- カテゴリ欄に `OK` と入っていた行が1件 → 正しい大項目に修正
- 資産の日付見出しが1つ誤っていた（年が1年ずれていた）→ 修正

変換結果は「年別実績表」シートと突合して検算済み。差分は上記の補完分とピボット漏れ3行のみで、
それ以外は全年・全項目で一致している。

⚠️ **取り込みは1台だけで行う。** 2台で取り込むと同じ内容が二重に入る（他の端末へは同期で配る）。

---

## 7. 開発と検証のやり方

ビルドは無い。ファイルを直接編集する。

動作確認は Playwright + Chromium で、CSV を取り込んだ状態を作ってから画面を叩くのが確実。
ブラウザに実データを入れずに済むので手元も汚れない。

```python
# 実ファイルをルーティングで差し込み、sync.js だけスタブにする
pg.route("**/sync.js",    lambda r: r.fulfill(status=200, content_type="text/javascript", body="/* stub */"))
pg.route("**/money.css",  lambda r: r.fulfill(status=200, content_type="text/css",        body=read("money.css")))
pg.route("**/money.app.js",lambda r: r.fulfill(status=200, content_type="text/javascript",body=read("money.app.js")))
pg.route("**/money.html", lambda r: r.fulfill(status=200, content_type="text/html",       body=read("money.html")))
pg.goto("https://example.test/money.html")
pg.set_input_files("#importFile", [...4本のCSV...])
pg.wait_for_timeout(6000)   # 取り込みは数秒かかる
```

- `pageerror` と `console` の error を必ず拾う。
- 画面ごとに代表的な数値（`#fNet`, `#mtotal`, `#logCount`, `#dataCount`）を読んで回帰を見る。
- スマホ幅は `viewport={"width":420,"height":900}`。
- グラフは `#expChart rect` / `#chart path` の要素数で描画の有無を見る。

`/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を `executable_path` に渡す
（`playwright install` は不要）。

---

## 8. デプロイ手順

**このコンテナから GitHub API への書き込みはできない**（`gh` コマンドも未インストール、
プロキシが `api.github.com` を弾く）。読み取りは `raw.githubusercontent.com` から可能。

Claude Code 側で git が使えるなら `git push` が一番早い。
使えない場合はブラウザ操作で上げる。実績のある手順は次のとおり。

1. `https://github.com/19tmnr89-design/life-log/upload/main` を開く
2. ページ内のJSで `File` オブジェクトを組み立て、`DataTransfer` 経由で
   `#upload-manifest-files-input` に `input.files = dt.files` で入れ、`change` を発火
3. コミットメッセージは `input[name="message"]` にネイティブsetterで値を入れて `input` を発火
4. `Commit changes` ボタンをクリック
5. `api.github.com/repos/.../contents/<file>` を **ページ内 fetch で** 叩いてサイズとSHA-256を検証

### ハマりどころ

- `input.files = dt.files` の直後に `change` を発火して即 `input.files.length` を読むと 0 になる。
  代入と発火は分けて、代入直後に長さを確認する。
- ブラウザ拡張の接続がよく切れる。切れるとタブIDが変わり、`window` に置いた変数が消える。
  大きなデータを流し込むときは `localStorage` に分割して置くと、再読み込みされても残る。
- 長い文字列を1回のツール呼び出しで流し込むと途中で化けることがある（実際にキリル文字が混入した）。
  **5KB程度に分割し、投入後に長さと SHA-256 を必ず検証する。**
- コミット直後に `raw.githubusercontent.com` を読むと CDN キャッシュで古いものが返る。
  検証は `api.github.com` 側で行う。

---

## 9. いま未反映の変更 ⚠️

**定例の修正（第5章の内容）がリポジトリに入っていない。**
ローカルで実装・検証済みだが、ブラウザ拡張の接続が落ちていてアップロードできなかった。
利用者に zip（`money-teirei-fix.zip`）を渡してあるが、2026-09-18 時点で未反映。

| ファイル | リポジトリの現在 | あるべき姿 |
|---|---|---|
| `money.app.js` | 59,366 B / `7d7a5236...` | 61,874 B / `a085cd13c982fad90ae8db59f66a749aa0a6feb800ada3b1d995a25794a9b8d9` |
| `money.css` | 18,348 B / `af0ad653...` | 18,462 B / `4564652843ec8152d52519ca358ae3b50ed42c969ad36edc18d0985606d810b7` |
| `money.html` | 11,363 B / `4b76df91...` | 変更なし |

最初にやること: リポジトリの `money.app.js` の SHA-256 を確認し、`7d7a5236...` のままなら
上記の修正版を反映する。

---

## 10. 過去にはまった落とし穴

同じ踏み方をしないように記録しておく。

**分類マスタが複数できる**
`{k:"map"}` は1本のつもりだったが、端末ごとに作られたものが同期の和集合マージで複数残った
（1キーのものと81キーのもの）。`find()` で1本目だけ読むと大項目が丸ごと消える。
→ 読むときは全部の map を `Object.assign` で重ねる。書くときは一番中身の多いものに寄せる。

**iOS の拡大**
- `user-scalable=no` は iOS 10 以降無視される。テンキーの連打で拡大してしまう。
  → `button, a, label, .tile` に `touch-action: manipulation`
- 入力欄のフォントが16px未満だとフォーカス時に自動でズームする。
  → `@media (pointer:coarse)` で16px以上にする

**iOS の日付ピッカーが開かない**
非表示の `<input type="date">` に対する `showPicker()` は効かない。
→ 実物の `<input type="date">` をチップの上に絶対配置で重ねて透明にする。

**同期していない状態を「同期が壊れている」と誤認した**
記録は9,511件すべて入っていて、単に同期を一度も開始していなかっただけだった。
別端末では4桁の短いコードが入っていた。
→ まず端末のレコード数と同期ステータスを実際に読む。推測で直しに行かない。

**ピボットの取りこぼし**
Excel の年別実績表はピボットで、元データの一部の行を拾えていなかった。
変換結果と突合するときは「差が出る＝変換が間違い」とは限らない。

---

## 11. 宿題・今後やるなら

- **未反映の定例修正の反映**（第9章）— 最優先
- データ量が上限の80%を超えたら年別分割（第3章）
- 賞与のように年2回しかない項目は自動判定に入らない。「＋ 定例に項目を追加」から手で足す運用
- 資産のグラフと支出のグラフでツールチップの実装が別になっている。共通化の余地あり

---

## 12. 守ること

- **このリポジトリは公開。** 口座名・残高・取引内容・定例項目名・金額をコードやドキュメントに書かない。
  マスタは必ず取り込んだ記録から実行時に組み立てる。
- **同期コードをコード・コミットメッセージ・ドキュメントに書かない。** 唯一の鍵。
- **筋トレログを壊さない。** `sync.js` は共通なので、触るときは後方互換を保つ
  （`SYNC_FINGERPRINT` 未指定なら従来動作、という形にする）。
- CSV の取り込みは1台だけ。他の端末へは同期で配る。
